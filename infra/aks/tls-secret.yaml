# Generate a self-signed certificate with this command in your AKS cluster:
# kubectl create secret tls backstage-tls --cert=tls.crt --key=tls.key -n lighthouse --dry-run=client -o yaml | kubectl apply -f -
#
# Or if you have OpenSSL available:
# openssl req -x509 -newkey rsa:4096 -keyout tls.key -out tls.crt -days 365 -nodes -subj "/CN=backstage.20.103.52.125.nip.io"
# kubectl create secret tls backstage-tls --cert=tls.crt --key=tls.key -n lighthouse
#
# For now, this is a placeholder. The secret will be created via the kubectl command above.
